import Foundation
import Cocoa

// MARK: - JMSCore Module
// 合并后的核心服务模块，包含数据模型和基础服务

/// JMSCore模块的主要导出接口
public struct JMSCore {
    public static let version = "1.2.0"
    public static let name = "JMS Protocol Handler Core"
}
